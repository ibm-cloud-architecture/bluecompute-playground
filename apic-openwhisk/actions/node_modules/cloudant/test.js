var cloudant = require('./cloudant.js')('https://reader.cloudant.com');
var db = cloudant.db.use('alchemy');
db.search('enhanced','search', {q: 'trump', sort:'title<string>'}, function(err, data) {

  console.log(err, JSON.stringify(data))
  ;
})
